def keyPassword = ""
def storeFile = ""
def storePassword = ""

def secureProperties = new File("${project.rootDir}/secure.properties")

if (secureProperties.exists()) {

    Properties properties = new Properties()
    properties.load(project.rootProject.file('secure.properties').newDataInputStream())
    keyPassword = properties.getProperty("keyalias.password")
    storePassword = properties.getProperty("keystore.password")
    
} else {

    keyPassword = System.getenv("KEYALIAS_PASSWORD")
    storePassword = System.getenv("KEYSTORE_PASSWORD")
    
}
